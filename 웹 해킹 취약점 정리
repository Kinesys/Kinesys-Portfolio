1. SQL injection : 파라미터 즉 사용자 입력값에 SQL 공문을 삽입하여 어플리케이션에서 DB로 질의를 하게 되는데, 이때 공격자가 의도한 대로 임의의 정보를 주고 받는다.

2. Xpath injection : 보통 프로그램 코딩적으로 프로그램을 구성되어 있는데, 그것을 탐색하거나 실행할 때 경로가 필요하다. 그경로를 Xpath 라고 하며, 그 경로를 변조하거나 탈취하는 행위를 뜻한다.

3. Command injection : 공격자가 악의적으로 시스템을 구동하는 명령을 자신의 의도대로 조절하는 것이다.

4. XXE injection : 공격자가 XML 문서에서 External Entity를 이용해 공격자 의도하는 외부 URL을 실행 시킨다.

5. XSS(Cross Site Scripting) : 게시판이나 스크립트를 삽입할수 있는 공간에 악성행위를 할수 있는 스크립트를 삽입하는 공격이다.

6. CSRF(Cross Site Request forgery) : 특정 사이트의 동적인 기능에 악의적인 코드를 삽입한다. 특정한 행동을 시키고 실행하며, XSS 와는 다소 다르다. ( XSS = 단순 정보 탈취, CSRF : 악성 행동 수행)

7. 파일 업로드 취약점 : 파일 업로드 진행시, 원하지 않는 악성 파일을 업로드 한다. 일반적으로 이런 악성 파일을 ‘웹쉘’ 이라 한다. 커맨드 인젝션과 비슷한 파급력을 가진다.

8. 파일 다운로드 취약점 : 원래 서버의 특정 권한이 부여된 파일만 다운 받을 수 있는데, 취약점을 이용해 강제로 경로 조작을 하게 되고 여러 파일을 다운 받는다.

9. 파라미터 변조 취약점 : 원래 특정 입력정보를 넣어야 하는 부분에 강제로 다른 정보를 넣어 변조 시킨다. 특정 입력 값을 입력해야만 실행되는 기능이 있는데, 이것을 변조한다.

10. 웹 페이지 접근 제어 : 일반적으로 관리자 권한 같은 경우 관리자만 접근이 가능해야 하는데, 일반 사람들도 접근이 가능하도록 만든다. 

11. 페이지 내 중요 정보 누출 : 비밀번호나 ID입력 하는 부분은 매우 예민해서 *****로 변조되어 출력되는데, 이것을 실제 입력값이 보이게 만들어 제3자가 쉽게 탈취 하도록 만든다. 

12. WAS 취약점 : 웹 어플리케이션 서버 (WAS)의 취약점을 공격하는 행위이다.

13. 프레임워크 취약점 : framework 는 소프트웨어, 어플 개발을 수월하게 하기 위해 구체적 기능들에 해당하는부분의 설계와 구현을 재사용하도록 협업이된 형태로 제공하는 소프트웨어 환경을 말하는데, 이 개발 환경 자체를 공격하는 행위.
